# 變更紀錄 Changelog

本檔記錄「國際交流成效回報系統」每一版的變更。
格式參考 [Keep a Changelog](https://keepachangelog.com/)，版本號採語意化版本（[SemVer](https://semver.org/)）：`主版號.次版號.修訂號`。

- **主版號（major）**：不相容的重大改版、資料結構或流程的破壞性變更。
- **次版號（minor）**：新增功能或欄位，且向下相容。
- **修訂號（patch）**：修錯字、修 bug、樣式微調。

> 版本號不需手動維護：每次改動程式碼時，由做變更的 AI（Claude Code，或對話中的 Claude）依本次實際變更判斷 SemVer 等級，更新 `lib/version.ts` 的 `APP_VERSION`，並在下方補上一段該版紀錄。詳細規則寫在 `CLAUDE.md`。

## [Unreleased]
（尚未發布、開發中的變更先記在這裡）

## [1.3.1] - 2026-06-18
### 變更 Changed
- 後台「選項管理」四個區塊顯示順序調整為：學制 → 指標歸組 → 活動大類 → 活動類型（僅調整 JSX 區塊順序，邏輯、API、DB 不變）。

## [1.3.0] - 2026-06-18
### 新增 Added
- 公開 repo 前的安全把關：`scripts/security-check.mjs`（零相依密鑰掃描）、`.husky/pre-commit`（每次 commit 跑無 `--strict`，CRITICAL 才擋）、GitHub Actions `.github/workflows/security.yml`（push／PR 跑 `--strict` ＋ gitleaks 掃整個 git 歷史）、`SECURITY.md` 安全說明與公開前清單、`.env.example` 環境變數範本。`package.json` 新增 `security-check` script。
- `db/migrations/2026-06-18_units_identity.sql`：把 `units.id` 補成 `GENERATED BY DEFAULT AS IDENTITY` 並推進序列指標（可重複執行）。
### 修正 Fixed
- `units.id` 在 schema 仍為純 `bigint`、無預設值，導致後台「新增系所」（已改為不手動帶 id）實際會插入失敗。`db/schema.sql` 與新 migration 一併補上 identity，自動配號生效；種子 1~42 仍可顯式帶入（`by default`）。
### 變更 Changed
- `.gitignore` 補強忽略 `.env*.local` 並保留 `!.env.example`。

## [1.2.2] - 2026-06-18
### 修正 Fixed
- 學院彙整頁可能將「已送出」系所誤顯示為「填報中」、與後台不一致：根因是讀取期別／送出狀態時把查詢錯誤靜默吞掉，期別被當成 -1、送出紀錄變空陣列，導致各系一律落到「填報中／未填」。改為：`getOpenPeriod()` 查詢失敗時拋出（不再與「沒有開放期別」混為一談）；學院頁送出狀態讀取失敗時拋出而非回空陣列；`/api/college` 讀取失敗回 500，前端保留上一份正確資料不被洗掉。
  （狀態判斷邏輯本身已正確：讀目前開放期別的 submission，submitted→已送出、returned→已退回、no_activity→申報無活動、draft 且有資料→填報中；不要求一定要有 activities，亦未用 .single() 在多筆／零筆時靜默失敗。學院頁也已有 `export const dynamic = "force-dynamic"`。）

## [1.2.1] - 2026-06-18
### 修正 Fixed
- 後台新增／編輯／刪除系所後，前台「學院選單」與「學院頁系所卡片」因快取看不到變更：單位異動成功後呼叫 `revalidatePath` 重新整理入口、`/report` 與各學院頁，後台系所清單 GET 改以 `cache: "no-store"` 重抓。
### 變更 Changed
- 配合 `units.id` 改為資料庫自動遞增，後台新增系所不再手動計算並指定 `id`，改由資料庫自動配號（以 `insert(...).select("id")` 取回新 id）。

## [1.2.0] - 2026-06-17
### 新增 Added
- 後台「選項管理」新增「指標歸組」管理：可增刪／停用指標歸組並調整排序。新增的歸組為「人次」統計（人數加總），會在填報、學院、後台與 Excel 匯出新增一欄「〔名稱〕人次」。
- 活動大類的「指標歸組」下拉改為動態列出目前啟用的歸組（不再限定出國交流／研討會兩種）。
### Changed
- 全站指標欄位改為依「指標歸組」動態產生（填報頁小計、學院彙整、後台總覽、Excel 統計總表）。內建「出國交流（人次）」「研討會類（場次＋人數）」維持原計算與顯示。
- 新增彙總視圖 `v_unit_group_metrics`（各系 × 期別 × 歸組 的場次與人次）；解除 `options.metric_group` 的固定限制（改對應 `metric_groups.key`，由 API 驗證）。
- 移除誤入庫的巢狀重複專案資料夾 `intl-report/`（一份過時的全專案副本，會被一併編譯）。
### 升級需執行 Migration
- 於 Supabase SQL Editor 執行 `db/migrations/2026-06-17_metric_groups.sql`（新建 metric_groups 表、解除 options.metric_group 限制、建立 v_unit_group_metrics、寫入內建兩組）。

## [1.1.0] - 2026-06-17
### 新增 Added
- 後台新增「選項管理」分頁：可增刪／停用「學制、活動大類、活動類型」項目；活動大類可設定「指標歸組」（出國交流人次 vs 研討會場次/人數），活動類型連動所屬大類。
- 填報「國家/地區」改為可搜尋下拉：固定清單來自教育部國別代碼（203 國家/地區），可用中文／英文／代碼搜尋，亦允許自由輸入。
- 學院頁與後台總覽顯示「填報中」狀態（已有資料但尚未送出），並每 30 秒自動更新、可手動「立即更新」，不必等各系送出即可看到目前進度；填報頁亦每 30 秒同步全系共用清單。
### Changed
- 學制／活動大類／活動類型改由資料庫 `options` 表維護（取代硬編碼常數），API 端依此驗證；三指標彙總視圖改用大類的 `metric_group` 分組以支援自訂大類。
- 移除 `activities.degree` / `activities.category` 的固定 CHECK 限制（改由選項表與 API 驗證）。
### 升級需執行 Migration
- 於 Supabase SQL Editor 執行 `db/migrations/2026-06-17_options.sql`（新建 options 表、重建彙總視圖、移除舊 CHECK、寫入預設選項）。

## [1.0.0] - 2026-06-17
### 新增 Added
- 國際交流成效回報系統首次正式版。
- 系所端免登入填報：全系共用一份清單，任何人都能整理未送出內容，確認送出即整批鎖定。
- 每筆紀錄記錄填報人；填報頁可即時看到全系所有項目與三指標小計。
- 學院彙整檢視、國際處後台儀表板、Excel 匯出。
- 入口首頁底部頁尾：開發者／維護資訊、版權說明、版本號與最後更新日期。
